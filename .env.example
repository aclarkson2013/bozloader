# ============================================
# BOZLOADER CONFIGURATION
# ============================================
# Copy this file to .env and fill in your values
# ============================================

# Application Settings
# --------------------
APP_NAME=Bozloader
SECRET_KEY=change-this-to-a-random-string-use-python-secrets-module
FLASK_DEBUG=False

# App URL (used for links in emails)
APP_URL=https://upload.yourdomain.com

# Admin Configuration
# -------------------
# Comma-separated list of admin email addresses
# These users can access /admin to approve/deny uploads
ADMIN_EMAILS=admin@yourdomain.com

# Upload Instructions
# -------------------
# Customize the instructions shown on the upload page
# Use \n for line breaks
UPLOAD_INSTRUCTIONS_MOVIES=Upload your movie files here.\nPlease name files: MovieTitle (Year).ext\nExample: Inception (2010).mkv
UPLOAD_INSTRUCTIONS_TV=Upload your TV show files here.\nPlease name files: ShowName - S01E01.ext\nExample: Breaking Bad - S01E01.mkv

# Storage Paths (Docker container paths)
# --------------------------------------
# These are the paths INSIDE the container
# Map them to your actual storage in docker-compose.yml
PENDING_MOVIES_PATH=/data/pending/movies
PENDING_TV_PATH=/data/pending/tv
PLEX_MOVIES_PATH=/data/plex/movies
PLEX_TV_PATH=/data/plex/tv

# Database Path
# -------------
DATABASE_PATH=/app/database/bozloader.db

# Email Configuration (SMTP)
# --------------------------
# Required for sending notifications
EMAIL_ENABLED=True
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USE_TLS=True
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@yourdomain.com

# For Gmail: Use an App Password, not your regular password
# Go to: Google Account → Security → 2-Step Verification → App passwords
# Create an app password for "Mail" on "Other (Custom name)"

# Discord Webhook (Optional)
# --------------------------
# Leave blank to disable Discord notifications
# Create a webhook in your Discord server: Server Settings → Integrations → Webhooks
DISCORD_WEBHOOK_URL=

# Plex Integration
# ----------------
# Required for automatic library scans after approval
PLEX_URL=http://192.168.0.50:32400
PLEX_TOKEN=your-plex-token

# To find your Plex token:
# 1. Sign in to Plex Web App
# 2. Browse to any media item
# 3. Click "Get Info" → "View XML"
# 4. Look for "X-Plex-Token=" in the URL

# Library names must match exactly what's in Plex
PLEX_MOVIES_LIBRARY=Movies
PLEX_TV_LIBRARY=TV Shows
